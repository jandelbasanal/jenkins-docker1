pipeline {
    agent { dockerfile true }
    stages {
        stage('test') {
            steps {
                sh 'hostname'
                sh 'node --version'
                sh 'git --version'
            }
        }
    }
}
